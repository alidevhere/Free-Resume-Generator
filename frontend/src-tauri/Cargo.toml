[package]
name = "free-resume-generator-desktop"
version = "0.1.0"
description = "Desktop app for Free Resume Generator"
authors = ["Free Resume Generator"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
